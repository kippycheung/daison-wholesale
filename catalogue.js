/* ===========================================================
   Daison Wholesale — PUBLISHED catalogue
   -----------------------------------------------------------
   This file holds the products/categories/site-info that ALL
   visitors see on the live site.

   To update the live site:
     1. Open  /#/admin  and make your changes (add products,
        upload images, edit prices/categories/site info).
     2. Go to the "Data" tab  →  click  "Publish".
     3. It downloads a new  catalogue.js  — replace THIS file
        with it, then redeploy (drag the folder onto Vercel,
        or push to your git repo).

   Until you publish for the first time, the site shows the
   built-in starter catalogue. Leaving the line below commented
   out is fine.
   =========================================================== */

// window.__seedCatalogue = { categories: [...], products: [...], settings: {...} };
